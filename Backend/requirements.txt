# Versiones fijadas (convención de develop) a las verificadas funcionando
# contra el ClickHouse real el 2026-07-23.

# Web framework
fastapi==0.139.2
uvicorn[standard]==0.51.0

# Base de datos — ClickHouse (capa Plata del pipeline Tángara, solo lectura).
# El extra "async" instala aiohttp, requerido por get_async_client().
clickhouse-connect[async]==1.5.0

# Geometría — point-in-polygon sobre sectores.geojson.
# No se usa pygeohash: la columna `geo` se decodifica con geohashDecode()
# en la propia query de ClickHouse (ver 03-Arquitectura-Backend.md §4).
shapely==2.1.2

# Cache TTL en memoria (services/cache.py)
cachetools==7.1.5

# Configuración / variables de entorno
pydantic==2.13.4
pydantic-settings==2.14.2
python-dotenv==1.2.2
