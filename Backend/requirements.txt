# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Base de datos
asyncpg==0.29.0          # driver async para PostgreSQL
sqlalchemy[asyncio]==2.0.35  # ORM — mapea tablas a clases Python
alembic==1.13.2          # migraciones de base de datos

# Validación de datos
pydantic==2.9.2
pydantic-settings==2.5.2  # leer variables de entorno desde .env

# Autenticación JWT
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Calibración científica (PM2.5)
numpy==2.1.1
pandas==2.2.3
scikit-learn==1.5.2

# IA / Chatbot
langchain==0.3.1
langchain-openai==0.2.1
langchain-anthropic==0.2.1

# WebSockets (incluido en fastapi, pero explícito por claridad)
websockets==13.0.1
