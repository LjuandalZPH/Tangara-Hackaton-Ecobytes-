# Web framework
fastapi>=0.115
uvicorn[standard]>=0.30

# Cliente ClickHouse (soporta HTTPS/TLS y cliente async; el extra
# "async" instala aiohttp, requerido por clickhouse_connect.get_async_client)
clickhouse-connect[async]>=0.8

# Geometría (point-in-polygon sobre sectores.geojson)
shapely>=2.0

# Cache TTL en memoria
cachetools>=5.5

# Configuración / variables de entorno
pydantic>=2.9
pydantic-settings>=2.5
python-dotenv>=1.0
