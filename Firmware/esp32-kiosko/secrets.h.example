#pragma once

// Copiar este archivo a secrets.h (misma carpeta que el .ino) y completar
// antes de compilar. secrets.h no se versiona -- ver .gitignore.

inline constexpr const char* kWifiSsid = "TU_RED_WIFI";
inline constexpr const char* kWifiPassword = "TU_PASSWORD";

// Backend expuesto via Tailscale Funnel (HTTPS publico) -- el ESP32 no
// puede unirse a la tailnet, asi que no se usa la IP 100.x.y.z de
// Tailscale ni una IP de LAN. Sin puerto: Funnel sirve en el 443 estandar.
// Ejemplo: "https://tu-maquina.tu-tailnet.ts.net"
inline constexpr const char* kBackendBaseUrl = "https://tu-maquina.tu-tailnet.ts.net";
